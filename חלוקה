// Talya
X - List, void* (pointer to function)
X - Change all allocations to list
X - Remove airport count field from AirportManager struct

// Coral AND Talya:
X - int initManagerAndAirline(AirportManager* pManager, Airline* pCompany)
X - int initAirlineFromFile(Airline* pComp, AirportManager* pManager, const char* fileName)

// Coral
V - Files, Sort
V - int saveManagerToFile(const AirportManager* pManager, const char* fileName)
V - int saveAirlineToFile(const Airline* pComp, const char* fileName)
V - int initManager(AirportManager* pManager, const char* fileName)
